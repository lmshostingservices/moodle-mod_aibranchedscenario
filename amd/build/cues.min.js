define("mod_aibranchedscenario/cues",["exports"],function(e){"use strict";Object.defineProperty(e,"__esModule",{value:!0}),e.play=e.forDecision=void 0;
/**
   * The sound language.
   *
   * Five cues, synthesised rather than shipped. Nothing is downloaded, nothing is cached,
   * nothing has to be hosted, and the whole vocabulary is about forty lines - which also
   * means the one rule that matters can be enforced in code rather than in a style guide:
   *
   *   THERE IS NO BUZZER.
   *
   * A game-show buzzer on a wrong answer tells the learner they are doing a quiz, which is
   * the single thing this product is trying not to be. When a decision goes badly the
   * consequence says so - the room changes, somebody stops talking - and the sound under it
   * is low and quiet, the sound of something having gone wrong rather than of a klaxon
   * announcing it.
   *
   * The cues are deliberately unequal. If every good decision got the same chime, the chime
   * would stop meaning anything by the third one; scarcity is what makes the big moments
   * land. So an ordinary good choice gets one soft note, a genuinely strong one under
   * pressure gets a rising pair, and the full triad is kept for the end of the scenario and
   * nothing else.
   *
   * @module     mod_aibranchedscenario/cues
   * @copyright  2026 LMS Hosting Services
   * @license    https://www.gnu.org/licenses/gpl-3.0.html GNU GPL v3 or later
   */
const t={good:[[659.25,0,.3,.055]],strong:[[523.25,0,.26,.06],[783.99,.09,.34,.07]],danger:[[174.61,0,.55,.05]],incident:[[174.61,0,.45,.06],[130.81,.12,.55,.05]],flat:[[440,0,.22,.045],[440,.13,.22,.045]],complete:[[523.25,0,.36,.09],[659.25,.11,.36,.09],[783.99,.22,.4,.09]]};let n=null;e.play=(e,i)=>{const o=t[e],a=window.AudioContext||window.webkitAudioContext||null;if(!o||!i||!a)return!1;try{return n||(n=new a),"suspended"===n.state&&n.resume&&n.resume(),o.forEach(([e,t,i,o])=>{const a=n.currentTime+t,r=n.createOscillator(),c=n.createGain();r.type="sine",r.frequency.setValueAtTime(e,a),c.gain.setValueAtTime(1e-4,a),c.gain.exponentialRampToValueAtTime(o,a+.02),c.gain.exponentialRampToValueAtTime(1e-4,a+i),r.connect(c).connect(n.destination),r.start(a),r.stop(a+i+.02)}),!0}catch(e){return!1}};e.forDecision=(e,t,n)=>"positive"===e?n?"strong":"good":"negative"===e?t>=70?"incident":"danger":"flat"});
//# sourceMappingURL=cues.min.js.map
