define("mod_aibranchedscenario/scheme",["exports"],function(e){"use strict";Object.defineProperty(e,"__esModule",{value:!0}),e.watch=e.apply=void 0;
/**
   * Match the page the activity was dropped into.
   *
   * The activity ships its own palette, which means it can disagree with the theme around
   * it. On a dark Moodle theme a light palette is a white slab in a dark page, and that is
   * the single thing that most reads as "this plugin was not built for us".
   *
   * The usual fix - `@media (prefers-color-scheme: dark)` - is wrong in both directions
   * here. A light theme on a machine set to dark would get a dark activity inside a light
   * page, which is worse than the problem. A theme with its own dark toggle, which is how
   * most commercial Moodle themes actually do it, would get nothing at all, because the
   * operating system was never asked.
   *
   * So this does not ask the operating system. It looks at the page: the first ancestor
   * behind the activity that actually paints a background, and how light that colour is. A
   * dark page gets the dark palette whatever the reason it is dark, and a light page never
   * does. A theme that toggles at runtime is picked up too, because the observer below
   * watches the attributes those toggles change.
   *
   * @module     mod_aibranchedscenario/scheme
   * @copyright  2026 LMS Hosting Services
   * @license    http://www.gnu.org/copyleft/gpl.html GNU GPL v3 or later
   */
const t=e=>{if(!e)return null;const t=e.replace(/[^0-9.,\s/]/g,"").split(/[\s,/]+/).filter(e=>""!==e);if(t.length<3)return null;const[n,r,a]=t.map(Number),o=t.length>3?Number(t[3]):1;return[n,r,a,o].some(e=>isNaN(e))?null:{r:n,g:r,b:a,a:o}},n=e=>{if(!e)return"light";const n=(e=>{let n=e;for(;n&&1===n.nodeType;){const e=t(window.getComputedStyle(n).backgroundColor);if(e&&e.a>.5)return e;n=n.parentElement}return null})(e.parentElement||document.body),r=n&&(e=>{const t=e=>{const t=e/255;return t<=.03928?t/12.92:Math.pow((t+.055)/1.055,2.4)};return.2126*t(e.r)+.7152*t(e.g)+.0722*t(e.b)})(n)<.4?"dark":"light";return"dark"===r?e.dataset.aibsScheme="dark":delete e.dataset.aibsScheme,r};e.apply=n;e.watch=e=>{if(e){if(n(e),window.MutationObserver){const t=new MutationObserver(()=>n(e));[document.documentElement,document.body].forEach(e=>{e&&t.observe(e,{attributes:!0,attributeFilter:["class","style","data-theme"]})})}if(window.matchMedia){const t=window.matchMedia("(prefers-color-scheme: dark)"),r=()=>n(e);t.addEventListener?t.addEventListener("change",r):t.addListener&&t.addListener(r)}}}});
//# sourceMappingURL=scheme.min.js.map
